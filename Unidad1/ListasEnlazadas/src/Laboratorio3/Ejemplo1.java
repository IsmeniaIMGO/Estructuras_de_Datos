package Laboratorio3;

public class Ejemplo1 {

    public static void main(String[] args) {

        //Obtener los números de las posiciones impares de una lista enlazada simple de números


        ListaSimple lista = new ListaSimple();
        lista.agregarInicio(3);
        lista.agregarInicio(4);
        lista.agregarInicio(5);

        //ListaSimple nomerosImpares = obtenerImpares(lista);


    }

    /*public static ListaSimple obtenerImpares(ListaSimple lista){
        ListaSimple listaAux = new ListaSimple();

        for (int i = 0; 1 < lista.tamano; i++){
            if (i %2 != 0){
                listaAux.agregarInicio(lista.buscarLista(i));

            }
        }
      }*/




}
